name: test
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-18.04
          - ubuntu-20.04
        compiler:
          - gcc
          - clang
      fail-fast: false
    env:
      CC: ${{ matrix.compiler }}
    steps:
      - uses: actions/checkout@v2
      - uses: actions/cache@v2
        with:
          path: |
            ~/setup-dpdk.cache.txz
          key: ${{ matrix.os }}_${{ matrix.compiler }}_${{ hashFiles('.github/workflows/*.yml', 'action.yml', 'main.sh') }}
      - uses: ./
        with:
          dpdk-version: '20.11'
          spdk-version: '20.10'
          hugepages: 1024
      - name: list installed files
        run: |
          find /usr/local/bin -name 'dpdk*' -or -name 'spdk*'
          find /usr/local/lib -name 'librte_*' -o -name 'libspdk_*'
